---
# Get server repo.
- name: Clone server repository.
  git:
    repo: "{{ item.repo }}/{{ item.name }}.git"
    dest: "{{ server_directory }}/{{ item.name }}"
    clone: no
    update: yes
  with_items: "{{ servers }}"
